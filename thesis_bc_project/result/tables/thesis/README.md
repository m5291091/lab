# Thesis / Presentation Tables (Gate K0)

Generated by `scripts/generate_thesis_artifacts.py`. Each table is written as presentation-ready Markdown (`.md`) and machine-readable `.tsv`. **All table text is English.** See `TABLE_MANIFEST.tsv` for provenance.

Regenerate:
```bash
python3 scripts/generate_thesis_artifacts.py
```

## Tables

- **T1** `T1_*.md` / `.tsv` -- Graph Metadata (nodes, edges, degrees, directedness).
- **T2** `T2_*.md` / `.tsv` -- Main Performance (GPU_Opt vs tuned PathMerge, speedup, GTEPS, trials).
- **T3** `T3_*.md` / `.tsv` -- Ablation Summary (Hybrid BFS / Warp-Cooperative / Dual-Stream effects).
- **T4** `T4_*.md` / `.tsv` -- Memory Scalability (feasible batch, effective/sub-batch, status).
- **T5** `T5_*.md` / `.tsv` -- Correctness Summary (small Pass; memory-path stress Core Fail preserved).
- **T6** `T6_*.md` / `.tsv` -- Experimental Environment (hardware, software, bandwidth).

## Notes

- Status vocabulary: Success, Out of Memory, Failed, Pass, Core Fail, Supported with Limitations.
- The canonical memory-path stress result is preserved as **Core Fail** and is never relabeled as Pass.
- Failures are reported as `N/A (OOM)` / `N/A (failed)`, never as 0 seconds; `OOM_OR_FAIL (exit 137)` without an independent OOM record is reported as Failed, not upgraded to confirmed Out of Memory.
