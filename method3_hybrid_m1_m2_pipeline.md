# Method 3 Proposal
**Document ID:** BC-METHOD-03  
**Method ID:** M3  
**Title:** Method 1 + Method 2 の統合ハイブリッド手法

---

## 1. 目的
Method 1（複合縮約）と Method 2（2-core復元）を統合し、グラフ特性に応じて最も効果的な縮約戦略を適用する。  
単独手法よりも汎用性と実効性能を向上させる。

---

## 2. 手法の概要
1. 2-core 抽出（M2）
2. 2-core 上でさらに複合縮約（M1）
3. 縮約グラフで厳密 BC 計算
4. 段階的復元（M1逆変換 → M2逆変換）

---

## 3. 新規性
- 既存研究は個別縮約の提案が中心で、**多段縮約を厳密復元付きで統合運用**する体系は希少。
- 「どの順序で縮約すると最も有効か」という実践的知見を提供できる。

---

## 4. 高速化の論理
- M2 で外周構造（Degree-1）を削る
- M1 で内部の冗長構造（Degree-2 チェーン等）を削る
- 2段階で n', m' をより小さくできれば、O(n'm') の主計算がさらに短縮
- 前処理コスト増加はあるが、BC本体が十分重い場合は相殺可能

---

## 5. 想定される有効グラフ
- Degree-1 と Degree-2 が混在する大規模疎グラフ
- 道路網 + 周辺葉ノードを持つ現実グラフ
- 複合構造グラフ（複数縮約ルールが同時に効く）

---

## 6. リスクと制約
- 実装複雑性が最も高い
- 復元順序を誤ると厳密性が崩れる
- 単純グラフでは前処理過多で利益が薄くなる可能性

---

## 7. 評価計画
- M1, M2, M3 の横比較
- 精度（厳密一致）確認
- End-to-End 実行時間比較
- 縮約率・復元コスト・総合効率の分解評価
- ベースライン比較: `gpu_opt`

---

## 8. 実装ステップ

以下のステップで段階的に実装を進める。Method 1 と Method 2 の実装が完了していることを前提とする。

### Step 1: M2（2-core 抽出）→ M1（複合縮約）の連結パイプライン構築
- **目標**: Method 2 の 2-core 抽出の出力を Method 1 の入力として受け渡すインターフェースを構築する
- **入力**: 元のグラフ（CSR 形式）
- **出力**: 2段階縮約後のグラフ、M2 剥離情報、M1 縮約情報
- **実装内容**:
  1. M2 の 2-core 抽出（Degree-1 反復除去）を実行し、2-core グラフと剥離情報を取得
  2. 2-core グラフを M1 の入力形式に変換（頂点 ID リマッピング）
  3. M1 の縮約パイプラインを 2-core に対して実行（Degree-1 再除去 → Degree-2 チェーン圧縮 → 同一構造統合）
  4. 最終縮約グラフと各段階の縮約情報を保持
- **検証**: 2段階縮約後のグラフが M1 単体・M2 単体よりも頂点・辺数が少ないことを確認

### Step 2: 2段階縮約グラフ上での厳密 BC 計算（GPU）
- **目標**: 最も縮小されたグラフ上で GPU Brandes 法を実行する
- **入力**: Step 1 で得られた最終縮約グラフ
- **出力**: 縮約グラフ上の BC 値配列
- **実装内容**:
  1. 最終縮約グラフを `Graph` クラス互換形式に変換
  2. `brandes_gpu_opt` を実行
  3. BC 値を取得
- **検証**: 縮約グラフ上の BC 値が正常に計算されることを確認

### Step 3: 段階的復元（M1 逆変換 → M2 逆変換）の実装
- **目標**: 縮約グラフの BC 値から元グラフの正確な BC 値を、正しい順序で段階的に復元する
- **入力**: 縮約グラフの BC 値（Step 2）、M1 縮約情報、M2 剥離情報（Step 1）
- **出力**: 元グラフの全頂点に対する正確な BC 値
- **実装内容**:
  1. **M1 逆変換を先に実行**（縮約の逆順）:
     - 同一構造統合の逆変換（twin 頂点の BC 分配）
     - Degree-2 チェーンの逆変換（チェーン内部頂点の BC 復元）
     - Degree-1 除去の逆変換（M1 で追加除去した葉頂点の BC 復元）
  2. **M2 逆変換を後に実行**:
     - 2-core 抽出時の剥離頂点の BC を再帰的に復元
  3. BC 値を元の頂点 ID 空間に逆マッピング
- **検証**: 元グラフに対する直接計算（`brandes_sequential` または `brandes_gpu`）の結果と完全一致するか比較

### Step 4: 縮約順序の最適化実験
- **目標**: M1 と M2 の適用順序による効果の違いを検証し、最適な順序を決定する
- **入力**: 複数のグラフファイル
- **出力**: 順序別の縮約率・実行時間比較レポート
- **実装内容**:
  1. パターン A: M2 → M1（デフォルト: 2-core 抽出後に複合縮約）
  2. パターン B: M1 → M2（複合縮約後に 2-core 抽出）
  3. 各パターンで縮約率（頂点・辺の削減率）を比較
  4. 各パターンの End-to-End 実行時間を比較
  5. グラフ特性ごとに最適パターンを特定
- **検証**: 両パターンとも正確な BC 値を復元できることを確認

### Step 5: End-to-End パイプラインの統合と包括的ベンチマーク
- **目標**: M3 の完全パイプラインを構築し、M1・M2・ベースラインとの包括的比較を行う
- **入力**: 全評価用グラフセット
- **出力**: 包括的性能評価レポート
- **実装内容**:
  1. パイプライン全体を統合する関数を実装（`brandes_gpu_hybrid` として `brandes.h` に追加）
  2. 各フェーズの実行時間を個別に計測（M2 抽出 / M1 縮約 / BC 計算 / M1 復元 / M2 復元）
  3. M1, M2, M3 の3手法横比較
  4. `gpu_opt` ベースラインとの End-to-End 時間比較
  5. 縮約率・復元コスト・総合効率の分解評価
  6. グラフ種別ごと（road / web / social / synthetic）の最適手法を特定
- **検証**: 全グラフで正確性の一致を確認し、性能結果を `RESULT.md` に追記

### Step 6: 適応的手法選択ロジックの実装（オプション）
- **目標**: グラフ特性を自動分析し、最適な縮約戦略（M1 / M2 / M3）を自動選択する
- **入力**: 任意のグラフファイル
- **出力**: 選択された手法による BC 計算結果
- **実装内容**:
  1. グラフの Degree 分布を高速分析（O(n) で実行）
  2. Degree-1 割合と Degree-2 割合から最適手法を推定:
     - Degree-1 が多い → M2 が有効
     - Degree-2 チェーンが多い → M1 が有効
     - 両方多い → M3 が有効
     - どちらも少ない → ベースライン（`gpu_opt`）をそのまま使用
  3. 選択ロジックを実装し、自動実行
- **検証**: 自動選択の結果が手動選択の最適手法と一致することを確認

---

## 9. 修士論文としての位置づけ
- 3手法比較により論文構成が明快（単体2手法＋統合1手法）
- 実装・理論・評価の統合度が高く、修士研究として強い完成形を作れる