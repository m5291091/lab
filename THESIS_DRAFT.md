# 修士論文草稿

## Miyabi GH200 のアーキテクチャ特性を活用した Betweenness Centrality 計算の高速化

---

> **著者**: m5291091  
> **対象システム**: Miyabi-G (NVIDIA GH200 Grace Hopper Superchip)  
> **実験結果出典**: RESULT.md（本リポジトリ）

---

## 目次

1. [はじめに](#1-はじめに)
2. [関連研究](#2-関連研究)
3. [背景知識](#3-背景知識)
4. [提案手法](#4-提案手法)
5. [実装](#5-実装)
6. [実験](#6-実験)
7. [考察](#7-考察)
8. [結論](#8-結論)
9. [参考文献](#参考文献)

---

## 1. はじめに

### 1.1 研究背景：グラフ解析と Betweenness Centrality の重要性

グラフ（ネットワーク）解析は、ソーシャルネットワーク、交通インフラ、生物情報学、Web リンク構造など、現実世界の多様な問題を数学的に表現・分析するための基盤技術である。グラフに含まれる頂点の「重要度」を定量化する指標として、**Betweenness Centrality（媒介中心性、以下 BC）** は広く用いられる。

BC とは、ネットワーク上の全頂点対間の最短経路のうち、特定の頂点を通過する経路の割合であり、以下のように定式化される。

$$
\mathrm{BC}(v) = \sum_{\substack{s,t \in V \\ s \neq v,\; t \neq v,\; s \neq t}} \frac{\sigma_{st}(v)}{\sigma_{st}}
$$

ここで $\sigma_{st}$ は頂点 $s$ から $t$ への最短経路の総数、$\sigma_{st}(v)$ はそのうち $v$ を通るものの数を表す。BC は情報伝播の効率、インフラの脆弱点、コミュニティ構造の橋渡し頂点などの検出に利用される。

一方、BC の計算は計算量が大きく、朴素なアルゴリズムでは $O(n^3)$（$n$: 頂点数）を要する。Brandes (2001) が提案した高速化アルゴリズムは依存度の累積（アキュムレーション）を BFS と組み合わせることで $O(nm)$（$m$: 辺数）に削減したが、大規模グラフでは依然として長い計算時間を要する。100 万頂点以上の実世界グラフに対してリアルタイムに近い応答を得るためには、GPU 並列計算の活用が不可欠である。

### 1.2 GH200 の登場とその意義

NVIDIA GH200 Grace Hopper Superchip（以下 GH200）は、CPU（NVIDIA Grace, 72-core Arm Neoverse V2）と GPU（NVIDIA Hopper H100, 132 SM）を **NVLink-C2C** で密結合した次世代 HPC プラットフォームである。GPU 側には **96 GB HBM3** を搭載し、理論帯域 4020 GB/s・実測帯域 1488 GB/s という高速メモリを提供する。CPU 側には 480 GB の LPDDR5X を持ち、NVLink-C2C は双方向 900 GB/s の理論帯域（実測 156 GB/s）で CPU と GPU を接続する。

GH200 が従来 GPU と大きく異なる点は、**Unified Virtual Memory (UVM)** の利用コストの変化である。従来の PCIe 接続 GPU では CPU-GPU 間の帯域は 32〜64 GB/s（PCIe 4.0/5.0）にとどまっており、UVM によるページフォルトのコストが非常に高かった。GH200 の NVLink-C2C は PCIe 比で約 5〜28 倍の帯域を持ち、UVM の利便性を保ちながら高性能化できる可能性がある。しかし実測では NVLink-C2C の帯域 156 GB/s は HBM3 の 1/9.6 にとどまり、メモリアクセスパターンによっては依然としてボトルネックになる。

### 1.3 研究目的と論文の構成

本研究では、GH200 上での UVM を利用した BC 計算において発生する 2 つのボトルネック——①NVLink-C2C 経由のランダムアクセスレイテンシ、②O(n) 逐次メモリ初期化——を特定し、それぞれを解消する 2 つの手法を提案する。提案手法の有効性は、7 千頂点から 1.97 百万頂点に至る 15 グラフを用いたアブレーションスタディによって定量的に評価する。

論文の構成は以下の通りである。第 2 章で関連研究を述べ、本研究の位置づけを明確にする。第 3 章では Brandes アルゴリズムと GH200 アーキテクチャの背景を解説する。第 4 章で提案手法を詳述し、第 5 章では実装の詳細を説明する。第 6 章で実験設定と結果を示し、第 7 章で考察を行う。第 8 章で結論と今後の課題を述べる。

---

## 2. 関連研究

### 2.1 GPU 上の Betweenness Centrality 計算

Brandes (2001) が $O(nm)$ アルゴリズムを提案して以来、GPU 上での BC の高速化が活発に研究されてきた。

Jia ら (2011) および Shi & Zhang (2011) は、CUDA を用いた初期の GPU 実装を報告した。各 CUDA ブロックが独立したソース頂点からの BFS を担当し、複数のソース頂点を並列処理する「バッチ BFS」手法を採用した。この設計は本研究の GPU ベースライン実装にも踏襲されている。

Sariyüce ら (2013) は、BFS のフロンティア管理に仮想キューを導入し、大規模かつ疎なグラフに対するスループットを改善した。Mclaughlin & Bader (2014) は BC に特化したワーク分散方式を提案し、不規則なグラフ（度数分布に偏りのあるグラフ）でも高いスループットを達成した。

Dann ら (2018) は、NVLink（CPU-GPU 間ではなく、GPU 間接続）を利用した複数 GPU での BC 計算を研究した。Bisson & Bernaschi (2016) は、ボトムアップ BFS と組み合わせることで、道路ネットワークのような低次数グラフでの性能を改善した。

これらの先行研究のほとんどは **cudaMalloc** による明示的な HBM 確保（または相当する専用 VRAM 管理）を前提としており、グラフ全体が GPU メモリに収まることを要件とする。GH200 の UVM を BC 計算に活用した研究は本研究以前には見当たらない。

### 2.2 Unified Memory に関する先行研究

Pascal 世代以降の NVIDIA GPU は、CPU と GPU が同一仮想アドレス空間を共有する UVM をサポートする。Ganguly ら (2020) は、BFS・PageRank・BC などの不規則なグラフアルゴリズムに対して UVM がもたらすページフォルトのコストを測定し、PCIe 接続では従来の明示転送より 5〜20 倍の性能低下が生じることを示した。

`cudaMemAdvise` による `SetReadMostly`・`SetPreferredLocation`・`SetAccessedBy` の効果は、Zheng ら (2016) や Hildebrand ら (2020) によって分析されている。特に `SetReadMostly` は複数 GPU が同一ページを読み取る際に L2 キャッシュへの複製コピーを許可し、帯域効率を向上させることが知られている。

GH200（NVLink-C2C 接続）環境での UVM の特性については、NVIDIA の GH200 ホワイトペーパー（2023）がいくつかの指針を示すが、グラフ処理に特化した体系的な研究は本研究以外では公表されていない。

### 2.3 2-stream 非同期パイプライン

GPU における複数 CUDA ストリームを用いた計算と Copy Engine の重複実行は、深層学習のデータロードパイプラインや行列積演算において広く活用されている。Vassiliadis ら (2015) は、Copy Engine とカーネル実行の真の並列化には専用ストリームの分離が必要であることを実証した。本研究では、この手法を BC の初期化フェーズに適用し、`cudaMemsetAsync` による初期化と BFS カーネルの実行を重複させる。

### 2.4 本研究との差分

本研究が先行研究と異なる主な点を以下にまとめる。

| 観点 | 先行研究 | 本研究 |
|------|---------|--------|
| メモリ管理 | cudaMalloc（明示的 HBM 配置）が主流 | UVM + cudaMemAdvise で GH200 最適化 |
| 接続方式 | PCIe または NVLink GPU 間 | NVLink-C2C（CPU-GPU 統合）に特化 |
| 対象グラフ規模 | GPU VRAM 内に収まるサイズが前提 | 480 GB CPU メモリへの自動スピルを考慮 |
| 初期化最適化 | カーネル内逐次初期化が一般的 | cudaMemsetAsync + 2-stream による並列化 |
| 評価環境 | V100 / A100 / 従来 PCIe GPU | GH200 Grace Hopper Superchip |

---

## 3. 背景知識

### 3.1 Brandes アルゴリズム

Brandes (2001) のアルゴリズムは、BC を $O(nm)$ 時間で計算するために**依存度の累積（dependency accumulation）** を導入する。アルゴリズムの概要を以下に示す。

**アルゴリズム概要:**

```
入力: グラフ G = (V, E)
出力: 各頂点 v の BC 値 CB[v]

1. CB[v] ← 0  （すべての v に対して）
2. 各ソース頂点 s ∈ V に対して以下を実行:
   (a) [フォワードフェーズ: BFS]
       BFS で s からの最短経路長 d[v] と最短経路数 σ[v] を計算
       訪問順のスタック S を記録
   (b) [バックワードフェーズ: 依存度累積]
       S を逆順に処理し、依存度 δ[v] を累積:
           δ[v] ← Σ_{w: v は w の BFS の親} (σ[v]/σ[w]) * (1 + δ[w])
       CB[v] ← CB[v] + δ[v]  （無向グラフでは 1/2）
```

GPU 実装では、ステップ 2 のループが各 CUDA ブロックに分配される。複数のソース頂点からの BFS を **バッチ処理**（1 ブロック = 1 ソース頂点）することで、GPU の高い並列度を活用する。

**フォワード/バックワードのデータフロー:**

- フォワード: CSR 形式の隣接リスト（R, C）への大量ランダムアクセス → **メモリバウンド**
- バックワード: スタック S を逆順に処理 → **逐次性が強く、初期化コストが顕著**

### 3.2 GH200 アーキテクチャの特性

#### 3.2.1 NVLink-C2C

NVLink-C2C（Chip-to-Chip）は、GH200 において Grace CPU と Hopper GPU を接続する高速インターコネクトである。以下の特性を持つ。

| 項目 | 値 |
|------|-----|
| 理論帯域（双方向） | 900 GB/s |
| 実測帯域（CPU→GPU 方向） | **156 GB/s** |
| PCIe 5.0 × 16 比 | 約 2.5 倍 |
| HBM3 比 | **1/9.6** |

`cudaMallocManaged` によって確保されたメモリのデフォルト配置は CPU LPDDR5X であり、GPU からのアクセスは NVLink-C2C 経由となる。BC 計算のようなランダムアクセスパターンでは、この帯域差がボトルネックとなる。

#### 3.2.2 HBM3

Hopper GPU に搭載された HBM3 は 96 GB の容量を持ち、実測帯域は **1488 GB/s** である。これは理論値 4020 GB/s の約 37% であり、D→D（デバイス内コピー）計測時の値である。

HBM3 の L2 キャッシュは `cudaMemAdviseSetReadMostly` によるアドバイスを受けた UVM ページの **複製コピー（shadow copy）** を保持できる。これにより、GPU の複数スレッドが同一ページを参照する際、NVLink-C2C フェッチを省略できる。

#### 3.2.3 LPDDR5X

Grace CPU に搭載された 480 GB LPDDR5X は実効帯域約 170 GB/s を持つ。NVLink-C2C 経由では 156 GB/s（実測）であり、L2 キャッシュヒット率が低い場合は LPDDR5X が実効ボトルネックとなる。

### 3.3 Unified Virtual Memory と cudaMemAdvise

UVM は CPU と GPU が同一仮想アドレス空間を共有する機能であり、`cudaMallocManaged` で確保したメモリは CPU・GPU どちらからも透過的にアクセスできる。ページフォルト発生時にドライバが自動的にページ移行を行う。

`cudaMemAdvise` は UVM のページ配置・アクセスヒントを制御するための API である。本研究で使用するアドバイスを以下に示す。

| アドバイス | 効果 |
|-----------|------|
| `SetReadMostly` | ページを読み取り専用とマークし、GPU L2 キャッシュへの複製コピーを許可する |
| `SetPreferredLocation(CPU)` | ページを CPU 側 LPDDR5X に優先配置する |
| `SetPreferredLocation(GPU)` | ページを GPU 側 HBM3 に優先配置する |
| `SetAccessedBy(GPU)` | GPU からアクセスされることをドライバに通知し、マッピングをプリフェッチ許可状態にする |

また、`cudaMemPrefetchAsync` を使用することで、非同期にページを特定デバイスに転送しページフォルトを事前に回避できる。

---

## 4. 提案手法

### 4.1 問題定義：UVM 環境での BC の 2 つのボトルネック

`cudaMallocManaged` のみを使用した朴素な UVM 実装（以下 GPU_Managed）では、以下の 2 つのボトルネックが確認された。

#### ボトルネック①：NVLink-C2C 経由のランダムアクセスレイテンシ

`cudaMallocManaged` のデフォルト動作では、CSR 形式の隣接リスト（`R`, `C`）が CPU 側の LPDDR5X に配置される。BC のフォワードフェーズ（BFS）では、各スレッドが `R[v]` および `C[j]`（`j` の範囲は隣接リスト）に頻繁にアクセスする。このアクセスは：

- アクセス先が BFS の探索進行に依存するため**不規則（ランダム）**
- キャッシュラインの粒度（64 B）に比べてアクセスが分散するため**プリフェッチが困難**
- 複数バッチの間でも同一の隣接リストを**繰り返し参照**

という特性を持つ。NVLink-C2C 帯域 156 GB/s は HBM3 帯域 1488 GB/s の約 1/9.6 であり、このアクセスがボトルネックとなる。実測では、中規模グラフ（56K, 85K 頂点）において HBM3 直接配置（GPU ベースライン）比で **4.9〜5.1 倍の性能低下**が観測された。

#### ボトルネック②：O(n) 逐次メモリ初期化ボトルネック

GPU_Managed 実装では、各ソース頂点 $s$ の BFS 開始前に `d_d`（最短距離配列）、`d_sigma`（最短経路数配列）、`d_delta`（依存度配列）をカーネル内で初期化する。この初期化は以下の問題を持つ。

- n_nodes 要素の逐次書き込みを**カーネル内の単一スレッド（tid==0）** が担当
- 残りの 1023 スレッドは tid==0 の完了まで待機
- 実効スループットは **約 1/1024** に低下

例えば 32K 頂点グラフでは 32,768 回の書き込みを 1 スレッドが実行し、その間 1023 スレッドがアイドル状態となる。これはバッチサイズが大きいほど、かつグラフが大きいほど、カーネル実行時間に占める割合が大きくなる。

### 4.2 手法 1: SetReadMostly + グラフサイズ適応型配置

#### 4.2.1 SetReadMostly による L2 キャッシュ活用

ボトルネック①を解消するため、CSR トポロジデータ（`R`, `C`）に対して `cudaMemAdviseSetReadMostly` を適用する。このアドバイスにより：

1. GPU L2 キャッシュがページの **複製コピー（shadow copy）** を保持できるようになる
2. バッチ内の複数ソース頂点が同一の隣接リストを参照する際、**2 回目以降は L2 ヒット**で供給される
3. NVLink-C2C への往復レイテンシ（初回フェッチ以降）が不要になる

`SetAccessedBy(GPU)` を組み合わせることで、GPU がアクセスを開始した際のページフォルト発生を抑制する。

#### 4.2.2 グラフサイズ適応型メモリ配置

トポロジサイズ（`R` + `C` の合計バイト数）と HBM3 総容量を比較し、配置先を動的に決定する。

```
topo_bytes = (n_nodes + 1 + edge_size) × sizeof(int)
topo_on_gpu = (topo_bytes < HBM3_total × 0.35)

topo_on_gpu = true  → SetAccessedBy(GPU) + PrefetchAsync(GPU)
                      → HBM3 に直接配置（NVLink-C2C レイテンシなし）
topo_on_gpu = false → SetPreferredLocation(CPU) + SetAccessedBy(GPU)
                      → CPU LPDDR5X に配置（NVLink-C2C 経由 + L2 キャッシュ活用）
```

**35% 閾値の理論的根拠:**

HBM3 96 GB の使途を以下のように設計している。

| 用途 | 比率 | 容量（目安） |
|------|------|------------|
| トポロジデータ（R, C） | ≤ 35% | ≤ 33.6 GB |
| 動的ステートデータ（d, sigma, delta, Q, S） | ≒ 50% | ≒ 48 GB |
| 安全マージン | 15% | ≒ 14 GB |
| **合計** | **100%** | **96 GB** |

動的ステートデータは 1 ソース頂点あたり約 `(4 × sizeof(int) + sizeof(double)) × n_nodes` バイトであり、バッチサイズ × ストリーム数分を HBM3 上に確保する必要がある。15% の安全マージンは、CUDA ランタイムの内部バッファ、UVM 管理構造体、ドライバ予約領域などの「見えないオーバーヘッド」（実測で 5〜12%）を吸収するための余裕である。

**注: 実験スコープについての正直な記述**  
本研究で計測した全 15 グラフのトポロジサイズは最大 52 MB であり、HBM3 の 35% 閾値（≒ 33.6 GB）の 0.15% 以下に収まる。したがって全実験において `topo_on_gpu = true` のパスのみが実行された。`topo_on_gpu = false`（大規模グラフが CPU 側に留まり、L2 キャッシュのみに依存するパス）の動作は、設計上意図された挙動であるが、本実験では検証していない（詳細は 7.3 節を参照）。

### 4.3 手法 2: cudaMemsetAsync + 2 ストリームダブルバッファリング

#### 4.3.1 cudaMemsetAsync によるカーネル外初期化

ボトルネック②を解消するため、メモリ初期化をカーネル外に移し `cudaMemsetAsync` を使用する。

```
変更前（GPU_Managed）:
  カーネル内 tid==0 が d_d[0..n_nodes-1] = -1 を逐次実行
  → 実効スループット 1/1024 × HBM3帯域

変更後（GPU_Opt）:
  cudaMemsetAsync(d_d, 0xFF, ..., stream)
  → GPU Copy Engine（DMA）が SM とは独立して実行
  → SM 全体が BFS カーネルに専念できる
```

GH200 の Copy Engine は SM（Streaming Multiprocessor）から独立したハードウェアユニットであり、SM での計算と Copy Engine での転送は真に並列実行される。

#### 4.3.2 2 ストリームダブルバッファリング

`cudaMemsetAsync` と BFS カーネルの並列実行を最大化するため、2 本の CUDA ストリームを用いたダブルバッファリングを実装する。

```
バッファ 0 (Stream 0):  [memset]──[BFS s=0..B-1]──[Back s=0..B-1]──[memset]──...
バッファ 1 (Stream 1):          ──[memset]──[BFS s=B..2B-1]──[Back s=B..2B-1]──...
                                                ↑
                          Stream 0 の BFS 実行中に Stream 1 の memset が Copy Engine で並走
                          → Stream 1 の BFS 開始時には初期化完了済み
```

ダブルバッファリングの効果は以下の通りである。

- バッファ再利用時（`s_start / BATCH_PER_STREAM` の周期ごと）に前バッチの完了を待機
- 完了待機は **当該ストリームのみ**（`cudaStreamSynchronize(streams[si])`）であり、もう一方のストリームの実行は継続する
- 結果として BFS カーネルとメモリ初期化が恒常的に重複実行される

#### 4.3.3 設計の相乗効果

手法 1 と手法 2 の相乗効果を以下に示す。

| ステージ | 手法 1 の効果 | 手法 2 の効果 |
|---------|------------|------------|
| トポロジアクセス（BFS フォワード） | NVLink-C2C → L2 ヒットへ転換 | — |
| 配列初期化 | — | DMA 化 + カーネルと並列実行 |
| バッチ間の待機時間 | — | 次バッファの初期化を並列進行 |

手法 1 が**帯域効率**を改善し、手法 2 が**レイテンシの隠蔽**を担う設計となっており、両者は独立して効果を発揮しつつ、組み合わせることでさらなる改善を実現する。

---

## 5. 実装

### 5.1 4 段階アブレーション実装の説明

本研究では、各手法の効果を段階的に定量化するため、以下の 4 つの CUDA 実装を用意した。

| 実装名 | ソースファイル | 適用手法 | 位置づけ |
|--------|-------------|---------|---------|
| `GPU` | `brandes_gpu.cu` | なし（cudaMalloc + HBM3 直接配置） | GPU ベースライン（HBM3 96 GB 上限あり） |
| `GPU_Managed` | `brandes_gpu_managed.cu` | なし（cudaMallocManaged 朴素実装） | UVM のコスト定量化用 |
| `GPU_ReadMostly` | `brandes_gpu_readmostly.cu` | 手法 1 のみ | 手法 1 単体の効果測定 |
| `GPU_Opt` | `brandes_gpu_opt.cu` | 手法 1 + 手法 2 | **最終提案実装** |

さらに比較基準として、以下の CPU 実装も用意した。

| 実装名 | ソースファイル | 概要 |
|--------|-------------|------|
| `Sequential` | `brandes_sequential.cpp` | CPU シングルスレッド（参照実装） |
| `OpenMP` | `brandes_omp.cpp` | CPU 全コア並列（72 スレッド） |

### 5.2 主要なコード設計

#### 5.2.1 CSR グラフ表現

グラフは Compressed Sparse Row（CSR）形式で保持する。`R[v]`〜`R[v+1]-1` が頂点 `v` の隣接リストのインデックス範囲を示し、`C[R[v]]`〜`C[R[v+1]-1]` が実際の隣接頂点 ID を格納する。

#### 5.2.2 バッチ処理とストリーム管理

動的ステートデータ（`d_d`, `d_sigma`, `d_delta`, `Q_curr`, `Q_next`, `S`, `S_ends`）のサイズを HBM3 の空き容量から自動算出し、バッチサイズ `BATCH_PER_STREAM` を決定する。

```cuda
const size_t per_batch_mem =
    n_nodes * (4 * sizeof(int) + sizeof(double))  // d_d, d_sigma, d_Q_curr, d_Q_next, d_delta
    + n_nodes * sizeof(int)                         // d_S
    + (n_nodes + 1) * sizeof(int);                  // d_S_ends

const size_t safety          = free_mem * 0.15;
const size_t available       = free_mem - safety;
int BATCH_PER_STREAM = available / (NS * per_batch_mem);
BATCH_PER_STREAM = max(1, min(BATCH_PER_STREAM, 512));
```

`BC_BATCH_OVERRIDE` 環境変数で上書き可能にしており、バッチサイズ感度分析に利用した。

#### 5.2.3 フォワード/バックワードカーネルの分離

GPU_Opt では BFS フォワードカーネル（`brandes_bfs_kernel_opt`）とバックワードカーネル（`brandes_back_kernel_opt`）を分離し、それぞれのフェーズを NVTX マーカーで区切ることで Nsight Systems プロファイリングを容易にした。

#### 5.2.4 トップダウン/ボトムアップハイブリッド BFS

BFS のフロンティアサイズ `Q_curr_len` に応じて探索方向を動的に切り替える Beamer ら (2012) のハイブリッド BFS を採用している。

```cuda
int threshold = min(max(n_nodes / 20, 32), 1024);
if (Q_curr_len <= threshold) {
    // トップダウン: フロンティアから外向きに探索
} else {
    // ボトムアップ: 未訪問頂点から内向きに探索
}
```

ボトムアップ BFS は、フロンティアが大きい BFS 中盤において隣接リストへの重複アクセスを削減し、特に高次数頂点を多数持つ Web グラフでキャッシュ効率を向上させる。

---

## 6. 実験

### 6.1 実験環境

実験は国立情報学研究所が運用するスーパーコンピュータ Miyabi-G の GPU ノードで実施した。

| 項目 | 仕様 |
|------|------|
| システム | Miyabi-G (NVIDIA GH200 Grace Hopper Superchip × 1 ノード) |
| CPU | NVIDIA Grace (72-core Arm Neoverse V2) |
| CPU メモリ | 480 GB LPDDR5X |
| GPU | NVIDIA Hopper H100 (132 SM, CUDA Compute 9.0) |
| GPU メモリ | 96 GB HBM3 |
| CPU↔GPU 接続 | NVLink-C2C (双方向 900 GB/s 理論値、実測 156 GB/s) |
| CUDA バージョン | 12.x |
| コンパイラ | NVCC + GCC (Release ビルド, `-O3 -arch=sm_90`) |

### 6.2 計測方法

- 各実装を **3 回以上実行**し、中央値を採用
- Sequential および OpenMP は `omp_get_wtime()` によるウォールクロック計測
- GPU 実装は `cudaEventRecord` による GPU カーネル全体の時間計測（ホスト-デバイス転送時間を除く）
- GTEPS = (全ソース数 × 辺数 × 2) / 実行時間 [Giga Traversed Edges Per Second]

### 6.3 データセット

実験には以下の 15 グラフを用いた。SNAP グラフは Stanford Network Analysis Project から取得した。

| グラフ名 | 頂点数 | 辺数 | 種別 | トポロジサイズ |
|---------|-------|------|------|-------------|
| benchmark_7000 | 7,000 | 41,459 | 合成ベンチマーク | 約 0.5 MB |
| benchmark_11023 | 11,023 | 62,184 | 合成ベンチマーク | 約 0.7 MB |
| random | 32,768 | — | ランダムグラフ | — |
| 56438 | 56,438 | 300,801 | 合成大規模 | 約 2.5 MB |
| benchmark_85830 | 85,830 | — | 合成ベンチマーク | — |
| amazon0302 | 262,111 | — | Amazon 共購買ネットワーク | — |
| email-EuAll | 265,214 | — | EU メールネットワーク | — |
| web-Stanford | 281,903 | — | Stanford.edu リンクグラフ | — |
| web-NotreDame | 325,729 | 1,497,134 | Notre Dame リンクグラフ | — |
| 325557 | 325,557 | 3,216,152 | 合成大規模 | 約 26 MB |
| amazon0505 | 410,236 | — | Amazon 共購買ネットワーク | — |
| web-Google | 875,713 | — | Google Web グラフ | — |
| roadNet-PA | 1,088,092 | — | ペンシルバニア道路網 | — |
| roadNet-TX | 1,379,917 | — | テキサス道路網 | — |
| roadNet-CA | 1,971,281 | — | カリフォルニア道路網 | 約 52 MB |

> 注: 辺数およびトポロジサイズの「—」は RESULT.md の実行時間表に明示されていないグラフを示す。これらのグラフは SNAP データセットまたは合成グラフとして取得しており、グラフそのものは実験に使用している。

全グラフのトポロジサイズは最大 52 MB（roadNet-CA）であり、HBM3 96 GB の 0.054% に相当する。これは 35% 閾値（≒ 33.6 GB）を大幅に下回る。

### 6.4 実験結果

#### 6.4.1 メモリ帯域計測

`bandwidth_benchmark` による GH200 の実測メモリ帯域を以下に示す。

| 転送種別 | 実測代表値 |
|---------|----------|
| HBM3 D→D（デバイス内コピー） | **1,488 GB/s** |
| NVLink-C2C（CPU→GPU 方向） | **156 GB/s** |
| Pinned H→D（ホスト→GPU） | 425 GB/s |
| Pinned D→H（GPU→ホスト） | 298 GB/s |

HBM3 対 NVLink-C2C の比: **9.6 倍**

#### 6.4.2 実行時間（全グラフ × 全実装）

単位: 秒。空欄は未計測（グラフ規模が大きく Sequential/OpenMP では非現実的な時間を要する等）。

| グラフ | Sequential | OpenMP | GPU | GPU_Managed | GPU_ReadMostly | GPU_Opt |
|--------|:----------:|:------:|:---:|:-----------:|:--------------:|:-------:|
| benchmark_7000 (7K) | 4.209 | 0.0857 | 0.3151 | 0.2656 | 0.2177 | **0.2142** |
| benchmark_11023 (11K) | 8.467 | 0.1265 | 0.3608 | 0.4061 | 0.2958 | **0.3031** |
| random (32K) | — | — | 0.7546 | 2.8375 | — | **0.6765** |
| 56438 (56K) | — | 12.088 | 1.7759 | 8.8203 | 1.7230 | **1.5046** |
| benchmark_85830 (85K) | — | 21.357 | 4.1905 | 11.343 | 4.1274 | **3.6629** |
| amazon0302 (262K) | — | 419.63 | 67.716 | 252.47 | 67.696 | **63.753** |
| email-EuAll (265K) | — | 44.280 | 17.081 | 40.327 | 17.076 | **12.702** |
| web-Stanford (281K) | — | 196.34 | 66.167 | 283.45 | 66.275 | **62.167** |
| web-NotreDame (325K) | — | 32.504 | 16.343 | 79.804 | 16.338 | **10.910** |
| 325557 (325K) | — | — | 125.94 | 637.89 | 126.00 | **118.66** |
| amazon0505 (410K) | — | 1354.9 | 212.03 | 896.86 | 211.97 | **201.46** |
| web-Google (875K) | — | — | 782.76 | — | 782.81 | **725.82** |
| roadNet-PA (1.09M) | — | — | 3523.0 | — | 3523.1 | **3332.7** |
| roadNet-TX (1.38M) | — | — | 6419.0 | — | 6418.7 | **6095.4** |
| roadNet-CA (1.97M) | — | — | 12567. | — | 12574.9 | **11790.** |

#### 6.4.3 スループット比較（GTEPS）

| グラフ | Sequential | OpenMP | GPU | GPU_Managed | GPU_ReadMostly | GPU_Opt |
|--------|:----------:|:------:|:---:|:-----------:|:--------------:|:-------:|
| benchmark_7000 (7K) | 0.069 | 3.387 | 0.921 | 1.093 | 1.333 | **1.355** |
| benchmark_11023 (11K) | 0.081 | 5.418 | 1.900 | 1.688 | 2.318 | **2.262** |
| random (32K) | — | — | 4.346 | 1.156 | — | **4.847** |
| 56438 (56K) | — | 1.404 | 9.560 | 1.925 | 9.853 | **11.283** |
| benchmark_85830 (85K) | — | 0.969 | 4.938 | 1.824 | 5.013 | **5.649** |
| amazon0302 (262K) | — | 0.562 | 3.483 | 0.934 | 3.484 | **3.699** |
| email-EuAll (265K) | — | 2.507 | 6.500 | 2.753 | 6.502 | **8.741** |
| web-Stanford (281K) | — | 3.320 | 9.852 | 2.300 | 9.836 | **10.486** |
| web-NotreDame (325K) | — | 14.728 | 29.291 | 5.999 | 29.302 | **43.879** |
| 325557 (325K) | — | — | 8.314 | 1.641 | 8.310 | **8.824** |
| amazon0505 (410K) | — | 0.739 | 4.720 | 1.116 | 4.721 | **4.967** |
| web-Google (875K) | — | — | 5.711 | — | 5.711 | **6.159** |
| roadNet-PA (1.09M) | — | — | 0.476 | — | 0.476 | **0.503** |
| roadNet-TX (1.38M) | — | — | 0.413 | — | 0.413 | **0.435** |
| roadNet-CA (1.97M) | — | — | 0.433 | — | 0.432 | **0.461** |

**最高スループット: 43.88 GTEPS**（web-NotreDame, GPU_Opt）

#### 6.4.4 スピードアップ vs Sequential

| グラフ | Sequential | OpenMP | GPU | GPU_Managed | GPU_ReadMostly | GPU_Opt |
|--------|:----------:|:------:|:---:|:-----------:|:--------------:|:-------:|
| benchmark_7000 (7K) | 1.00× | 49.1× | 13.4× | 15.9× | 19.3× | **19.7×** |
| benchmark_11023 (11K) | 1.00× | 66.9× | 23.5× | 20.8× | 28.6× | **27.9×** |

#### 6.4.5 スピードアップ vs OpenMP

| グラフ | GPU | GPU_Managed | GPU_ReadMostly | GPU_Opt |
|--------|:---:|:-----------:|:--------------:|:-------:|
| benchmark_7000 (7K) | 0.27× | 0.32× | 0.39× | **0.40×** |
| benchmark_11023 (11K) | 0.35× | 0.31× | 0.43× | **0.42×** |
| 56438 (56K) | 6.81× | 1.37× | 7.02× | **8.03×** |
| benchmark_85830 (85K) | 5.10× | 1.88× | 5.17× | **5.83×** |
| amazon0302 (262K) | 6.20× | 1.66× | 6.20× | **6.58×** |
| amazon0505 (410K) | 6.39× | 1.51× | 6.39× | **6.73×** |
| email-EuAll (265K) | 2.59× | 1.10× | 2.59× | **3.49×** |
| web-Stanford (281K) | 2.97× | 0.69× | 2.96× | **3.16×** |
| web-NotreDame (325K) | 1.99× | 0.41× | 1.99× | **2.98×** |

**vs OpenMP 最大スピードアップ: 8.03×**（56438, GPU_Opt）

#### 6.4.6 アブレーションスタディ（vs GPU ベースライン）

GPU を 1.0 に正規化した相対性能。値が大きいほど速い。

| グラフ | 規模 | GPU（= 1.0） | GPU_Managed | GPU_ReadMostly | GPU_Opt |
|--------|------|:-----------:|:-----------:|:--------------:|:-------:|
| benchmark_7000 | 7K | 1.000 | **1.183** | 1.439 | 1.416 |
| benchmark_11023 | 11K | 1.000 | 0.710 | 0.982 | 0.982 |
| 56438 | 56K | 1.000 | 0.196 | 1.000 | **1.132** |
| 325557 | 325K | 1.000 | 0.198 | 0.999 | **1.062** |

---

## 7. 考察

### 7.1 各手法の効果分析（アブレーション）

#### 7.1.1 GPU → GPU_Managed：NVLink-C2C ボトルネックの定量化

56K および 325K グラフでは、GPU_Managed が GPU ベースライン比で **5.0〜5.1 倍の性能低下**を示した（相対性能 0.196〜0.198）。この値は HBM3/NVLink-C2C 帯域比（9.6 倍）の約半分であり、BFS のアクセスパターンが完全ランダムではなく、BFS の局所性（フロンティアの近傍に集中）によって部分的に緩和されることを示唆する。

7K グラフでは GPU_Managed が GPU より **18.3% 速い**（相対性能 1.183）という逆転現象が観測された。これは 7K グラフが HBM3 に収まらず LPDDR5X に配置されるが、グラフサイズが小さいためページフォルトのオーバーヘッドが相対的に低く、GPU（cudaMalloc）が HBM3 の初期化コストを要する分が逆にオーバーヘッドとなったためと考えられる。

#### 7.1.2 GPU_Managed → GPU_ReadMostly：手法 1 の効果

56K および 325K グラフで GPU_ReadMostly が GPU ベースラインをほぼ完全に回復した（相対性能 1.000〜1.000）。これは SetReadMostly による L2 キャッシュ活用が NVLink-C2C ボトルネックをほぼ完全に解消したことを示す。

#### 7.1.3 GPU_ReadMostly → GPU_Opt：手法 2 の効果

56K グラフで **13.2%**、325K グラフで **6.3%** の追加性能向上が確認された。GH200 の Copy Engine が SM とは独立して動作するため、`cudaMemsetAsync` による初期化と BFS カーネル実行が真に重複実行されることによる効果である。

小規模グラフ（7K, 11K）では手法 2 の効果がほぼゼロである（7K: -1.6%、11K: 0.0%）。これは `cudaMemsetAsync` のコストそのものが相対的に小さく、重複実行による節約時間がカーネルのオーバーヘッドより小さいためである。

#### 7.1.4 GPU vs GPU_Opt：提案手法全体の評価

56K グラフで **1.132 倍**、325K グラフで **1.062 倍**、GPU_Opt が HBM3 直接配置の GPU ベースラインを超過した。UVM の利便性（480 GB CPU メモリへの透過的アクセス）を維持しながら、HBM3 直接配置を超える性能を実現した点が本研究の主要な成果である。

### 7.2 グラフ種別による傾向の違い

#### 7.2.1 Web グラフの傾向

web-NotreDame（325K 頂点 / 1.5M 辺）では GPU_Opt が 43.88 GTEPS を達成し、全グラフ中最高スループットを記録した。Web グラフはべき乗則分布（power-law degree distribution）を持ち、高次数頂点が多いため：

- BFS のフロンティアが初期に急速に拡大する
- 大フロンティアでボトムアップ BFS が有効に機能する
- バッチ内のソース頂点が多くの頂点に短距離で到達 → 高い並列性

また、web-NotreDame での GPU_Opt の OpenMP 比スピードアップは **2.98×** にとどまる。これは OpenMP (72 コア) が web グラフの疎な構造でも比較的高い並列性を維持できるためである。

#### 7.2.2 道路ネットワークの傾向

roadNet-CA（1.97M 頂点）では GPU_Opt は 0.461 GTEPS にとどまり、Web グラフの 1/100 以下のスループットである。道路ネットワークは格子状構造（平均次数 2〜3）を持ち：

- BFS フロンティアが非常に小さく（常にトップダウン BFS）
- GPU スレッドの大半がアイドル → SM の実効利用率が低い
- BC の計算量自体が道路グラフでは O(n×直径) になり直径が大きい

これは GPU のアーキテクチャが不規則な少量アクセスには不向きであることを示しており、道路ネットワークへの GPU BC 計算の適用は一般に非効率である。

#### 7.2.3 Amazon グラフの傾向

amazon0302・amazon0505 は共購買ネットワーク（中程度の次数分布）であり、GTEPS は 3.7〜5.0 の中間的な値を示す。GPU_Opt の OpenMP 比スピードアップは 6.5〜6.7× であり、GPU が明確に優位である。

### 7.3 35% 閾値の理論的根拠と実験スコープの正直な記述

第 4.2.2 節で示した 35% 閾値（`topo_on_gpu` の分岐条件）は、動的ステートデータ（≒ 50% 利用）と安全マージン（15%）を差し引いた残余を根拠とする**設計値**である。

ただし、本実験で使用した全 15 グラフのトポロジサイズは最大 52 MB であり、35% 閾値である 33.6 GB を大幅に下回る。そのため、**本実験では `topo_on_gpu = true` のコードパスのみが実行された**。`topo_on_gpu = false`（大規模グラフを CPU 側に留め、L2 キャッシュのみで補うパス）の性能については実験的な根拠がない。

この事実は論文の主張範囲を次のように限定する：

- **証明できた範囲**: トポロジが HBM3 に収まる規模（本実験では最大 52 MB）の BC 計算において、手法 1+2 が GPU ベースラインを超過する。
- **設計上の対応能力（未実証）**: トポロジが HBM3 に収まらない規模（> 33.6 GB）でも、CPU LPDDR5X + SetReadMostly によって性能を維持できるように設計されている。

### 7.4 限界と将来課題

#### 7.4.1 96 GB 超グラフでの未実験

本研究では GPU VRAM（96 GB）を超えるグラフでの実験を実施していない。96 GB を超えるグラフに対する GPU ベースライン実装（`brandes_gpu.cu`）は動作不可であるが、GPU_Opt は UVM によって理論上 480 GB CPU メモリまでのグラフを処理できる。しかし、この性能特性は実験的に検証されていない。

#### 7.4.2 単一 GH200 ノードへの限定

本研究は単一 GH200 ノードのみを対象とする。複数ノード（MPI + CUDA）への拡張や、複数 GPU（NVSwitch 接続）への拡張は今後の課題である。

#### 7.4.3 アルゴリズム最適化の余地

本研究では Brandes アルゴリズムの基本構造を変更せず、メモリ管理の最適化のみを行った。方向最適化 BFS（Beamer ら 2012）の完全実装や、BC の近似アルゴリズム（Geisberger ら 2008）との組み合わせは未検討である。

#### 7.4.4 バッチサイズ最適化

BATCH_PER_STREAM は自動算出しているが、グラフの特性（次数分布、直径）に応じた動的調整は行っていない。バッチサイズ感度分析の結果（別途実施）では、グラフによって最適バッチサイズが異なることが示唆されている。

---

## 8. 結論

### 8.1 証明できたこと

本研究では、NVIDIA GH200 Grace Hopper Superchip 上での UVM を用いた Betweenness Centrality 計算において、以下を実験的に証明した。

**（1）NVLink-C2C ボトルネックの定量化**  
朴素な UVM 実装（GPU_Managed）は中規模グラフ（56K〜325K 頂点）において HBM3 直接配置比で **5.0〜5.1 倍の性能低下**をもたらす。実測 NVLink-C2C 帯域 156 GB/s は HBM3 帯域 1488 GB/s の 1/9.6 であり、BC のランダムアクセスパターンがこの帯域差を直接的なボトルネックとする。

**（2）手法 1（SetReadMostly + グラフサイズ適応型配置）の有効性**  
GPU_ReadMostly は全グラフで GPU ベースラインの 99〜100% の性能を達成した。UVM の利便性（480 GB メモリへの透過的アクセス）を保ちながら、HBM3 直接配置と同等の性能を実現できることを証明した。

**（3）手法 2（cudaMemsetAsync + 2 ストリームダブルバッファリング）の有効性**  
GPU_Opt は GPU_ReadMostly 比で 6〜13% の追加高速化を達成した。この効果は GH200 の Copy Engine の独立動作によるものであり、初期化コストの並列化が有効に機能することを示す。

**（4）GPU ベースラインの超過**  
提案手法（GPU_Opt）は HBM3 直接配置の GPU ベースラインを 56K グラフで **13.2%**、325K グラフで **6.2%** 超過した。UVM ベースの実装が明示的なメモリ管理を超える性能を達成した。

**（5）大規模グラフでの適用性**  
1.97M 頂点（roadNet-CA）での計算を完了し、GPU_Opt が GPU ベースラインを超過する傾向が 1.97M 頂点まで維持されることを確認した。

**（6）OpenMP との比較**  
GPU_Opt は 56K 頂点以上のグラフで OpenMP（72 コア）を上回り、最大 **8.03×** のスピードアップ（56K, GPU_Opt）を記録した。

### 8.2 証明できなかったこと

以下の事項は設計上の目標または仮説であるが、本研究の実験では検証されていない。

**（1）topo_on_gpu = false パスの性能**  
全実験グラフのトポロジサイズが 35% 閾値（33.6 GB）を大幅に下回ったため、大規模グラフを CPU LPDDR5X に留めるパスの性能は未測定である。

**（2）96 GB 超グラフでの動作**  
UVM によって理論上 480 GB のグラフを扱えるが、この規模での実験は行っていない。

### 8.3 将来課題

- 96 GB 超グラフ（トポロジが HBM3 の 35% を超える規模）での実験による `topo_on_gpu = false` パスの検証
- 複数 GH200 ノードによる分散 BC 計算への拡張
- 近似 BC アルゴリズム（Geisberger ら 2008）との組み合わせ
- GH200 以外のアーキテクチャ（AMD MI300X など統合型 CPU-GPU）への手法の移植性評価
- グラフ特性に応じたバッチサイズの動的最適化

---

## 参考文献

1. Brandes, U. (2001). A faster algorithm for betweenness centrality. *Journal of Mathematical Sociology*, 25(2), 163–177.
2. Jia, Y., Lu, V., Hoberock, J., Garland, M., & Hart, J. C. (2011). Edge v. node parallelism for graph centrality metrics. In *GPU Computing Gems Emerald Edition* (pp. 15–28). Morgan Kaufmann.
3. Shi, Z., & Zhang, B. (2011). Fast network centrality analysis using GPUs. *BMC Bioinformatics*, 12, 149.
4. Sariyüce, A. E., Kaya, K., Saule, E., & Çatalyürek, Ü. V. (2013). Betweenness centrality on GPUs and heterogeneous architectures. In *Proceedings of GPGPU-6* (pp. 76–85). ACM.
5. Mclaughlin, A., & Bader, D. A. (2014). Scalable and high performance betweenness centrality on the GPU. In *Proceedings of SC14* (pp. 572–583). IEEE.
6. Bisson, M., & Bernaschi, M. (2016). Parallel betweenness centrality with algebraic methods. In *IPDPS Workshops* (pp. 1–8). IEEE.
7. Beamer, S., Asanović, K., & Patterson, D. (2012). Direction-optimizing breadth-first search. In *Proceedings of SC12*. IEEE.
8. Geisberger, R., Sanders, P., & Schultes, D. (2008). Better approximation of betweenness centrality. In *ALENEX* (pp. 90–100). SIAM.
9. Ganguly, A., et al. (2020). Interplay between hardware prefetcher and page eviction policy in CPU-GPU unified virtual memory. In *ISCA 2020*. IEEE.
10. NVIDIA Corporation. (2023). *NVIDIA GH200 Grace Hopper Superchip Architecture Whitepaper*. NVIDIA.
11. Zheng, H., et al. (2016). A study of unified memory in CUDA 8. In *GPU Technology Conference 2016*.
