# Method 1 Proposal
**Document ID:** BC-METHOD-01  
**Method ID:** M1  
**Title:** 複合グラフ縮約 + 正確な BC 復元（GPU 計算）

---

## 1. 目的
Betweenness Centrality (BC) の厳密計算に対して、前処理でグラフを縮約し、縮約グラフで計算後に元グラフのBCを正確に復元することで、全体実行時間を短縮する���

---

## 2. 手法の概要
本手法では、以下を組み合わせる。

1. Degree-1 頂点除去（peeling）
2. Degree-2 チェーン圧縮（path/chain compression）
3. 同一構造頂点の統合（identical/twin vertex merging）
4. 縮約グラフ上で厳密 BC 計算
5. 復元規則に基づく元グラフ BC の再構成

---

## 3. 新規性
- 既存研究では、上記要素の一部は個別に提案されているが、**GPU向け厳密BCパイプラインとして統合評価**した事例は限定的。
- 特に、Degree-2 チェーン圧縮と正確復元を中心に、実装・評価を包括的に行う点に意義がある。

---

## 4. 高速化の論理
- Brandes法の計算コストは概ね O(nm)。
- 縮約後のグラフサイズを n', m' とすると、主要計算は O(n'm') に低下。
- 前処理と復元は通常 O(n+m) オーダであり、BC本体より軽い。
- よって、**n' と m' を十分に削減できるグラフでは大幅高速化**が期待できる。

---

## 5. 想定される有効グラフ
- 道路網系（Degree-2 チェーンが多い）
- 木構造・準木構造を含む疎グラフ
- ペンダント構造（Degree-1）が多いネットワーク

---

## 6. リスクと制約
- 密グラフでは縮約余地が小さく、効果は限定的。
- 復元規則の正確性検証が最重要。
- 前処理ルールの適用順序で効果が変動する可能性あり。

---

## 7. 評価計画
- 正確性: 元グラフ直計算との一致検証
- 性能: End-to-End 時間（前処理+計算+復元）
- 縮約率: 頂点・辺の削減率
- グラフ特性別比較: road / web / social / synthetic
- ベースライン比較: `gpu_opt` を含む複数実装

---

## 8. 実装ステップ

以下のステップで段階的に実装を進める。各ステップは前のステップの成果物に依存する。

### Step 1: Degree-1 頂点除去（Peeling）の実装
- **目標**: グラフから Degree-1（葉）頂点を反復的に除去し、除去順序を記録する
- **入力**: CSR 形式のグラフ（`adjacencyList`, `adjacencyListPointers`）
- **出力**: 縮約後の CSR グラフ、除去頂点リスト（除去順序付き）
- **実装内容**:
  1. 全頂点の次数を計算
  2. Degree-1 の頂点をキューに追加
  3. キューから頂点を取り出し、隣接頂点の次数を更新（次数が 1 になれば再度キューへ）
  4. 除去した頂点と隣接辺の情報をスタックに記録（復元用）
  5. 残った頂点・辺から新しい CSR グラフを構築
- **検証**: 除去後のグラフに Degree-1 頂点が存在しないことを確認

### Step 2: Degree-2 チェーン圧縮（Path/Chain Compression）の実装
- **目標**: Degree-2 頂点の連鎖（チェーン）を単一辺に置換する
- **入力**: Step 1 で得られた縮約グラフ
- **出力**: さらに縮約された CSR グラフ、チェーン情報（元の経路長・頂点列）
- **実装内容**:
  1. Degree-2 頂点を検出
  2. 連続する Degree-2 頂点をたどり、チェーンの両端点を特定
  3. チェーン全体を 1 本の辺に置換（両端点間）
  4. チェーン情報（経路長、内部頂点列）を記録（復元用）
  5. 新しい CSR グラフを構築
- **検証**: 圧縮後のグラフに Degree-2 チェーンが残っていないことを確認

### Step 3: 同一構造頂点の統合（Identical/Twin Vertex Merging）の実装
- **目標**: 隣接構造が同一の頂点ペアを統合し、グラフをさらに縮約する
- **入力**: Step 2 で得られた縮約グラフ
- **出力**: さらに縮約された CSR グラフ、統合情報（マージされた頂点ペア）
- **実装内容**:
  1. 各頂点の隣接リストをソートしハッシュ化
  2. 同一ハッシュの頂点ペアを検出
  3. 隣接リストの完全一致を確認（ハッシュ衝突排除）
  4. 一致する頂点群を代表頂点に統合
  5. 統合情報を記録し、新しい CSR グラフを構築
- **検証**: 統合後のグラフに同一構造頂点が存在しないことを確認

### Step 4: 縮約グラフ上での厳密 BC 計算（GPU）
- **目標**: 縮約されたグラフ上で既存の GPU Brandes 法を実行する
- **入力**: Step 1〜3 で得られた最終縮約グラフ
- **出力**: 縮約グラフ上の BC 値配列
- **実装内容**:
  1. 縮約グラフを既存の `Graph` クラス互換形式に変換
  2. `brandes_gpu_opt` を縮約グラフに対して実行
  3. 縮約グラフの BC 値を取得
- **検証**: 縮約グラフ上の BC 値が正常に計算されることを確認

### Step 5: BC 復元（逆変換）の実装
- **目標**: 縮約グラフの BC 値から元グラフの正確な BC 値を再構成する
- **入力**: 縮約グラフの BC 値、Step 1〜3 で記録した縮約情報
- **出力**: 元グラフの全頂点に対する正確な BC 値
- **実装内容**:
  1. **Step 3 の逆変換**: 統合された頂点の BC を分配（twin 頂点間の復元公式を適用）
  2. **Step 2 の逆変換**: チェーン内部頂点の BC を計算（経路長と端点の BC から復元公式を適用）
  3. **Step 1 の逆変換**: Degree-1 頂点の BC を計算（スタックの逆順で、親頂点の BC と部分木サイズから計算）
  4. 復元順序は縮約の逆順（Step 3 → Step 2 → Step 1）
- **検証**: 元グラフに対する直接計算（`brandes_sequential` または `brandes_gpu`）の結果と完全一致するか比較

### Step 6: End-to-End パイプラインの統合とベンチマーク
- **目標**: 前処理（Step 1〜3）→ BC 計算（Step 4）→ 復元（Step 5）の一貫パイプラインを構築し、性能評価を行う
- **入力**: 任意のグラフファイル
- **出力**: End-to-End 実行時間、縮約率、正確性レポート
- **実装内容**:
  1. パイプライン全体を統合する関数を実装
  2. 各フェーズの実行時間を個別に計測
  3. 複数グラフ種別（road / web / social / synthetic）での評価
  4. `gpu_opt` ベースラインとの End-to-End 時間比較
  5. 縮約率（頂点・辺の削減率）の集計
- **検証**: 全グラフで正確性の一致を確認し、性能結果を `RESULT.md` に追記

---

## 9. 修士論文としての位置づけ
- 理論（縮約と復元）＋実装（GPU）＋評価（網羅実験）の構成が可能
- 実装難易度と成果のバランスが良く、修士論文テーマとして成立性が高い