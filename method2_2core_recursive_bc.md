# Method 2 Proposal
**Document ID:** BC-METHOD-02  
**Method ID:** M2  
**Title:** 2-core 分解ベースの厳密 BC 復元（GPU 計算）

---

## 1. 目的
2-core 分解を用いてグラフを簡約し、2-core上でBCを計算後、理論に基づいて元グラフのBCを再構成する。  
理論の実装検証と実システム上での性能評価を主眼とする。

---

## 2. 手法の概要
1. Degree-1 頂点の反復除去により 2-core を抽出
2. 2-core 上で厳密 BC 計算
3. 再帰的復元規則に基づき、剥離した頂点の BC を復元

---

## 3. 新規性
- 2-core 理論自体は先行研究が存在するが、**GPU実装での体系的性能評価**は未成熟。
- 理論を実験的に検証し、どのグラフ特性で実利が出るかを明確化する点に価値がある。

---

## 4. 高速化の論理
- 2-core によって頂点数・辺数が十分減少すれば O(nm) �� O(n'm') に短縮可能。
- ただし、Degree-1 が少ないグラフでは n', m' の削減効果が小さく、速度改善は限定的。
- 復元処理自体は軽量で、主計算に対する追加コストは小さい。

---

## 5. 想定される有効グラフ
- Degree-1 ペンダント頂点が多いグラフ
- 木に近いネットワーク
- 剥離で大きく縮小する構造を持つグラフ

---

## 6. リスクと制約
- 道路網のように Degree-2 主体で Degree-1 が少ない場合、効果は小さい可能性。
- 復元規則の適用ミスは正確性に直結。
- 「理論の実装検証」に留まり、手法自体の独創性が弱く見える可能性。

---

## 7. 評価計画
- 正確性: 直計算BCとの一致
- 性能: 2-core抽出 + BC + 復元の合計時間
- 剥離率: 2-core への縮小率
- グラフ種別ごとの効果比較
- ベースライン比較: `gpu_opt` および Method 1

---

## 8. 実装ステップ

以下のステップで段階的に実装を進める。各ステップは前のステップの成果物に依存する。

### Step 1: Degree-1 頂点の検出と反復除去の実装
- **目標**: グラフから Degree-1 頂点を反復的に除去し、2-core を抽出する
- **入力**: CSR 形式のグラフ（`adjacencyList`, `adjacencyListPointers`）
- **出力**: 2-core グラフ（CSR 形式）、剥離頂点リスト（除去順序付き）、各剥離頂点の親情報
- **実装内容**:
  1. 全頂点の次数を計算
  2. Degree-1 の頂点をキューに追加
  3. キューから頂点 v を取り出し、v の唯一の隣接頂点 u（親）を記録
  4. u の次数を 1 減算し、u が新たに Degree-1 になればキューへ追加
  5. 剥離情報（頂点 v、親 u、剥離時の部分木サイズ）をスタックに push
  6. 残った頂点（全て Degree ≥ 2）から 2-core の CSR グラフを構築
- **検証**: 2-core 内に Degree-1 頂点が存在しないこと、元グラフの頂点数 = 2-core 頂点数 + 剥離頂点数 であることを確認

### Step 2: 2-core 上での厳密 BC 計算（GPU）
- **目標**: 抽出した 2-core 上で既存の GPU Brandes 法を実行する
- **入力**: Step 1 で得られた 2-core グラフ
- **出力**: 2-core 頂点の BC 値配列
- **実装内容**:
  1. 2-core グラフを既存の `Graph` クラス互換形式に変換（頂点 ID のリマッピング）
  2. `brandes_gpu_opt` を 2-core グラフに対して実行
  3. BC 値を元の頂点 ID 空間に逆マッピング
- **検証**: 2-core のみを対象とした直接計算との BC 値一致を確認

### Step 3: 剥離頂点の BC 再帰的復元の実装
- **目標**: 剥離した Degree-1 頂点の BC 値を理論に基づいて正確に復元する
- **入力**: 2-core の BC 値（Step 2）、剥離情報スタック（Step 1）
- **出力**: 元グラフの全頂点に対する正確な BC 値
- **実装内容**:
  1. 剥離スタックを逆順に処理（最後に剥離した頂点から復元）
  2. 各剥離頂点 v について、親 u との関係から BC 値を計算:
     - v の BC 値 = (n - 1) × (部分木サイズ_v - 1) + Σ（部分木内の対の寄与）（理論式に基づく）
     - 親 u の BC 値に v 経由の最短経路寄与を加算
  3. 全頂点の BC 値が確定するまで反復
- **検証**: 元グラフに対する直接計算（`brandes_sequential` または `brandes_gpu`）の結果と完全一致するか比較

### Step 4: End-to-End パイプラインの統合と性能計測
- **目標**: 2-core 抽出 → BC 計算 → 復元の一貫パイプラインを構築し、性能評価を行う
- **入力**: 任意のグラフファイル
- **出力**: End-to-End 実行時間、剥離率、正確性レポート
- **実装内容**:
  1. パイプライン全体を統合する関数を実装（`brandes_gpu_2core` として `brandes.h` に追加）
  2. 各フェーズの実行時間を個別に計測（2-core 抽出 / BC 計算 / 復元）
  3. 複数グラフ種別での評価
  4. `gpu_opt` および Method 1 との End-to-End 時間比較
  5. 剥離率（2-core への縮小率）の集計
- **検証**: 全グラフで正確性の一致を確認し、性能結果を `RESULT.md` に追記

### Step 5: グラフ特性別の有効性分析
- **目標**: どのグラフ特性で 2-core 分解が最も効果的かを定量的に明らかにする
- **入力**: Step 4 の実験結果
- **出力**: グラフ特性別の効果分析レポート
- **実装内容**:
  1. 各グラフの Degree 分布を集計（特に Degree-1 の割合）
  2. 剥離率と高速化率の相関を分析
  3. 有効な閾値（剥離率 X% 以上で Y% の高速化が期待できる等）を特定
  4. Method 1 との効果の相補性を議論
- **検証**: 分析結果が実験データと整合していることを確認

---

## 9. 修士論文としての位置づけ
- 「最新理論のGPU実装評価」として学術的意義がある
- Method 1 と併用することで論文全体の完成度が高くなる